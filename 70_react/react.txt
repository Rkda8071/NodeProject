1. React : 페이스북에서 만든 Javascript 기반의 프론트엔드 라이브러리

2. React 특징
1) 컴포넌트라고 하는 코드 조각을 이용하여 UI를 구성
2) Virtual DOM (가상 DOM) 사용
    데이터가 바뀌었을 때 일단 그려 놓고, 그 다음에 비교를 하여
    바귄 부분만 찾아 업데이트 (DOM의 변화를 최소화함)
3) JSX(Javascript XML) : HTML 문법과 거의 유사하게 작성하는 Javascript
    const element = <div>Hello, React</div>
4) 상태관리, 라우팅, API 통신을 위한 추가 라이브러리

3.react 쓸 때 필요한 지식들
    spread 연산자
    비구조화 할당
    불변성 유지 : concat, filter, map
    import .. from, export default ...

4. react 환경 설정
1) Node.js
2) Yarn : 개선된 npm
3) npx create-react-app 71_basic

5. JSX 규칙 6가지

6. 리액트에서 다루는 데이터
1) props (Properties) : 부모 컴포넌트에서 자식 컴포넌트로 데이터 주기가 가능함
2) state
- 컴포넌트 내부에서 관리하는 데이터
- 리액트 16.8부터 함수형 컴포넌트에서 state 사용 가능
- Hook이라는 기능이 추가

7. 조건부 렌더링
- 조건이 참인 경우와 거짓인 경우에 다른 결과물을 렌더링 하는 것
-> 삼향 연산자
- 조건이 참인 경우에만 렌더링하는 경우 -> &&

8. Input 필드 상태 관리하기
- useState
- e.target.name, e.target.value

9. useRef() Hook
1) DOM에 직접 접근해야 하는 경우
2) 

*컴포넌트가 업데이트되는 경우
1) 부모 컴포넌트에서 넘겨주는 porps가 바뀔 때
2) 컴포넌트 자신이 갖고있는 state가 바뀔 때
3) 부모 컴포넌트가 리렌더링될 때

10. 배열 렌더링하기
